---
description: 'PM Orchestrator가 생성한 티켓들을 Wave 순서대로 실행하고, 각 에이전트에게 작업을 위임하여 프로젝트를 구현합니다.'
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
1. Always LOAD the FULL @.anyon/core/tasks/workflow.xml
2. READ its entire contents - this is the CORE OS for EXECUTING the specific workflow-config @.anyon/anyon-method/workflows/pm-executor/workflow.yaml
3. Pass the yaml path .anyon/anyon-method/workflows/pm-executor/workflow.yaml as 'workflow-config' parameter to the workflow.xml instructions
4. Follow workflow.xml instructions EXACTLY as written to process and follow the specific workflow config and its instructions
5. Save outputs after EACH section when generating any documents from templates
</steps>
