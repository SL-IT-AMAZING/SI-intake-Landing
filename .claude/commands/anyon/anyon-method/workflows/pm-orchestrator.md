---
description: '설계 문서(PRD, UX, UI, ERD, Architecture, TRD)를 분석하여 실행 가능한 티켓들과 실행 계획을 생성합니다. 에이전트 할당, 의존성 그래프, Wave 구성까지 자동으로 수행합니다.'
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
1. Always LOAD the FULL @.anyon/core/tasks/workflow.xml
2. READ its entire contents - this is the CORE OS for EXECUTING the specific workflow-config @.anyon/anyon-method/workflows/pm-orchestrator/workflow.yaml
3. Pass the yaml path .anyon/anyon-method/workflows/pm-orchestrator/workflow.yaml as 'workflow-config' parameter to the workflow.xml instructions
4. Follow workflow.xml instructions EXACTLY as written to process and follow the specific workflow config and its instructions
5. Save outputs after EACH section when generating any documents from templates
</steps>
